const root = "/vrnas-react"

export const routerPaths = {
    main: root,
    about: root + "/about"
}

export const translatePath: {[key: string] : string} = {
    "vrnas-react": "Home",
    "about": "About Us"
}